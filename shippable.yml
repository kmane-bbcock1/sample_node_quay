# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
 # pre_ci:
  #  - docker build -t=shippabledocker/shiphitchcock1_sample_node2_bb .

  pre_ci_boot:
    image_name: quay.io/shippablequay/sample_node_01
    image_tag: latest
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=quay.io/shippablequay/sample_node_01 .
    - docker push quay.io/shippablequay/shiphitchcock1_sample_node_quay_bb2

integrations:
  hub:
    - integrationName: ship-quay
      type: quay 
